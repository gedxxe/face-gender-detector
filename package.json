{
  "name": "face-gender-detector",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=public/bundle.js --format=esm --platform=browser --loader:.tsx=tsx --loader:.ts=ts --define:process.env.API_KEY=\\\"\"$API_KEY\\\"\" --jsx=automatic --external:react --external:react-dom/* --external:@google/genai"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}
